# ``Learning``

Библиотека для работы со скидками и промокодами в e-commerce приложениях.

@Metadata {
    @DisplayName("Learning")
    @Available("MyCompany", introduced: "1.0.0")
}
# ``Learning``

Давайте рассмотрим одну из важнейших структур данных – Массив

@Metadata {
    @DisplayName("Learning")
    @Available("MyCompany", introduced: "1.0.0")
}

## Ключевые понятия

- В массиве могут храниться элементы только ОДНОГО ТИПА (Либо необходимо объединять разные типы данных под один общий протокол)
- Элементы в массиве хранятся в том порядке, в каком были добавлены
- Элементы могут повторяться
- Благодаря индексу ([0]) мы можем обращаться к конкретному элементу. Индекс начинается с нуля

@Row {
    @Column {
        🚀 В массиве могут храниться элементы только **ОДНОГО ТИПА**
    }
    @Column {
        ⚡️ Элементы в массиве **хранятся в том порядке, в каком были добавлены**
    }
    @Column {
        🚀 Элементы **могут повторяться**
    }
    @Column {
        🚀 Благодаря индексу ([0]) мы можем обращаться к конкретному элементу. **Индекс начинается с нуля**
    }
}

## 1. Как создать массив ?

### Пустой массив

```swift
var names: [String] = []
```

### Массив с элементами
Можем не указывать тип данных – **: [Int]** так как массив уже самостоятельно понял, какого он типа исходя из тех данных, с которым мы его создали

```swift
let numbers = [0,1,2,3,4,5]
```

Стоит отдельно отметить, что в массив **names** мы ничего кроме строк (**String**) добавить не можем.

В массив **numbers** мы не можем добавить вообще ничего, даже **Int**. Как думаете почему ?

## 2. Как добавлять элементы в массив ?

#### Добавить новый элемент в самый конец массива

```swift
names.append("Lesha") // Теперь в массиве один элемент ["Lesha"]
```

#### Добавить элемент под определённый индекс

```swift
names.insert("Pasha", at: 0) // Теперь в массиве два элемента ["Pasha", "Lesha"]
```

#### Добавление нескольких элементов в конец массива:

```swift
let anotherNames = ["Ararat", "Gizelle"]
names.append(contentsOf: anotherNames) /// ["Pasha", "Lesha", "Ararat", "Gizelle"]
```

## 3. Доступ к элементам:

#### 3.1 По индексу:

```swift
let element = names[1] // Получим второй элемент в массиве т.е. переменная 'element' сейчас равна "Lesha"
```

#### Первый элемент:

```swift
let first = names.first // "Pasha"
```
#### Последний элемент:

```swift
let last = names.last // "Gizelle"
```

## 4. Изменение элементов:

#### Второй элемент в массивве теперь не "Lesha" а  "Loh"

```swift
names[1] = "Loh"
```

## 5. Удаление элементов:

#### Удаление по индексу:

```swift
names.remove(at: 0)
```

#### Удаление последнего:

```swift
names.removeLast()
```

#### Удаление первого:

```swift
names.removeFirst()
```

#### Удаленим все элементы:

```swift
names.removeAll()
```

## 6. Поиск значений

#### Удаление по индексу:

```swift
/// Индекс:  0        1        2        3         4
names = ["Vasya", "Sereja", "Masha", "Sasha", "Ararat"]
```

#### Проверка наличия элемента в массиве:

```swift
let isContainSereja = names.contains("Sereja") // true
```

#### Поиск индекса для конкретного элемента:

```swift
let indexOfSereja = names.firstIndex(of: "Sereja") // 1
```

## 7. Сортировка

#### Изменение массива внутри самого массива:

```swift
names.sort()
```

#### Создание отсортированной копии, а сам массив остался неизменным:

```swift
let sortedNames = names.sorted()
```

## 7. Проверка пустоты:

#### Массив пустой ?

```swift
let isEmpty = names.isEmpty // false
```

#### Количество элементов в массиве

```swift
let count = names.count
```

## 8. Как добавить элементы с разным типом данных в массив ?

#### Давайте представим, что у нас есть три структуры данных –

```swift
/// Ребёнок
struct Children: Human {
    let name: String
    var surname: String = ""
    let scores: [Int]
}

/// Учитель
struct Tutor: Human {
    let name: String
    var surname: String = ""
    let amount: Int
}

/// Директор
struct Director: Human {
    let name: String
    var surname: String
    let workExpirience: Int
}
```

Добавить их в один массив у нас не получится так как это всё разные типы данных, а массив может хранить только **один тип данных**

#### Ошибка – так сделать не получится, потому, что в массиве у нас лежат и 'Children' и 'Tutor' – это совершенно разные типы данных

```swift
let people = [
   Children(name: "Petya", scores: [3,4,5]),
   Tutor(name: "Marina", surname: "Ivanovna", amount: 10_000)
]
```

Для того, что бы мы, всё же, смогли хранить в одном массиве элементы с разными типами данных. Нам необходимо объединить их под общий протокол

##### Протокол 'Human' требует, чтобы у Структуры или Класса, закрытым этим протоколом, было поле 'name' и 'surname'

```swift
protocol Human {
    var name: String { get }        // get – Поле доступно ТОЛЬКО ДЛЯ ЧТЕНИЯ (let)
    var surname: String { get set } // get set – Поле доступно И ДЛЯ ЧТЕНИЯ И ДЛЯ ЗАПИСИ (var)
}
```

##### Мы не можем создать объект из протокола

```swift
let human = Human() // – Не сработает, создать объект можно только из Класса или Структуры (Children / Tutor и т.д.)
```

Давайте теперь укажем, что наш **Children**, **Tutor** и **Director** – Находятся под протоколом **Human**

Это позволит нам создать массив с типом данных **Human** и хранить в нём наших Хуманов

Обратите внимание – Не смотря на то, что в массив мы добавляем **Children**, **Tutor** и **Director** массив считает, что это ПРЕЖДЕ ВСЕГО Элементы под протоколом **Human**, а не конкретный тип данных (**Children**, **Tutor** и **Director** )

##### Список людей в классе (Каждый элемент массива это 'Human')

```swift
var humans: [Human] = [
    Children(name: "Petya", scores: [0,1,2]),
    Tutor(name: "Daria", amount: 100),
    Children(name: "Masha", scores: [3,2,1]),
    Director(name: "Жанна", surname: "Игоревна", workExpirience: 19)
]
```
